# gatekeeper egress allowlist (squid dstdomain). It only talks to the approval
# sidecar (internal, via NO_PROXY -- not through here); the sole external need is
# Google OAuth token verification, same as every other tool.
accounts.google.com
oauth2.googleapis.com
www.googleapis.com
openidconnect.googleapis.com
