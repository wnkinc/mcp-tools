# approval sidecar egress allowlist (squid dstdomain). The whole job: post the
# Approve/Deny card (slack.com chat.postMessage / discord.com channel message)
# and update it after a click (hooks.slack.com response_url; Discord updates
# in the interaction response, no extra call). Nothing else, ever.
slack.com
hooks.slack.com
discord.com
