# Pulumi program deps. Providers are pinned to the major versions this program's
# resource names target (cloudflare v6 renames Tunnel/Record; bump deliberately).
pulumi>=3.130,<4
pulumi-aws>=6.50,<7
pulumi-cloudflare>=5.49,<6
pulumi-random>=4.16,<5
