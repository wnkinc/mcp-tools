# THREAT-MODEL L5 (provenance): pin ALL deps so a fresh `pip install -r` reproduces a
# known set, not latest -- a floating dep silently un-audits itself on reinstall.
# Versions below = the currently-installed set; OSV/deps.dev vetting is still TODO
# (the deferred dep-vetting pipeline). Re-vet on any version bump.
#
# MCP framework + shared OAuth/.env plumbing:
fastmcp==3.4.2
mcp==1.28.0
python-dotenv==1.2.2
# Data engine. DuckDB is the parquet reader/writer + cache catalog (replaced the
# hand-rolled pyarrow store). pandas is the in-memory frame (OpenBB -> enforce -> store).
duckdb==1.5.4
pandas==3.0.3
# OpenBB market-data: core framework + the equity command extension + the yfinance
# provider extension. Commands and providers are SEPARATE extensions on purpose; adding
# a source = another provider ext, adding a data type = another command ext. yfinance +
# pyarrow are now TRANSITIVE (pulled by openbb-yfinance / unused respectively), so they
# are no longer listed here. After install, prebuild the accessor: `python -c "import openbb"`.
openbb-core==1.6.13
openbb-equity==1.6.2
openbb-yfinance==1.6.3
