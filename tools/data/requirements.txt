# Provenance: pin ALL deps so a fresh `pip install -r` reproduces a
# known set, not latest -- a floating dep silently un-audits itself on reinstall.
# Versions below = the currently-installed set; OSV/deps.dev vetting is still TODO
# (the deferred dep-vetting pipeline). Re-vet on any version bump.
#
# MCP framework + shared OAuth/.env plumbing:
fastmcp==3.4.2
mcp==1.28.0
python-dotenv==1.2.2
# Data engine. pandas is the in-memory frame; pyarrow is its parquet reader/writer
# (bars persist via DataFrame.to_parquet / read_parquet -- no separate store engine).
pandas==3.0.3
pyarrow==24.0.0
# OpenBB market-data: core framework + command extensions (one per DATA TYPE: equity,
# crypto, currency/FX) + provider extensions (one per SOURCE: yfinance, tiingo). Commands
# and providers are SEPARATE on purpose: a new data type = a command ext + a fn in feeds.py
# + a tool; a new source for an EXISTING data type = just a provider ext (no new code).
# yfinance is TRANSITIVE (pulled by openbb-yfinance). After install/adding an extension,
# prebuild the accessor: `python -c "import openbb"`. tiingo (the fixed provider) needs
# TIINGO_API_KEY.
openbb-core==1.6.13
openbb-equity==1.6.2
openbb-crypto==1.6.2
openbb-currency==1.6.2
openbb-yfinance==1.6.3
openbb-tiingo==1.6.1
# Direct vendor SDK (NOT an OpenBB provider — OpenBB has no databento extension). Opt-in
# alternative equity source, used via equity-ingest source="databento". Needs DATABENTO_API_KEY.
databento==0.80.0
