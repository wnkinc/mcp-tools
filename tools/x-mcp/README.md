# x-mcp

Our own thin FastMCP server exposing X (Twitter) read-only search/lookup, modeled on the example at https://github.com/xdevplatform/xmcp.git but written directly against `fastmcp` (see `server.py`).
